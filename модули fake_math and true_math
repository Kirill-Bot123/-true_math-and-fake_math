true_math


from math import inf


def true_divide(first, second):
    if second == 0:
        return inf
    else:
        return first / second


fake_math


def fake_divide(first, second):
    if second == 0:
        return 'Ошибка'
    else:
        return first / second
